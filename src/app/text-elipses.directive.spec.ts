import { TextElipsesDirective } from './text-elipses.directive';

describe('TextElipsesDirective', () => {
  it('should create an instance', () => {
    const directive = new TextElipsesDirective();
    expect(directive).toBeTruthy();
  });
});
